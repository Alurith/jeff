package testgen005

type Resource struct{ closed bool }

func (r *Resource) Close() { r.closed = true }

func (r *Resource) Use() error {
	if r.closed {
		return nil
	}
	return perform()
}

func perform() error { return nil }
