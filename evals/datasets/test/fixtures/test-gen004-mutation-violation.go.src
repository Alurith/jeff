package testgen004

type Cache interface {
	Put(string, string) error
}

type Reader struct{ cache Cache }

func (r Reader) Read(key string) (string, error) {
	if err := r.cache.Put(key, "refreshed"); err != nil {
		return "", err
	}
	return key, nil
}
