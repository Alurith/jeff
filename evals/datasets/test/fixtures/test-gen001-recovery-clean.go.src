package testgen001

import (
	"os"
	"strings"
)

func reload(active *[]string, path string) bool {
	data, err := os.ReadFile(path)
	if err != nil {
		return false
	}
	*active = strings.Split(strings.TrimSpace(string(data)), "\n")
	return true
}
