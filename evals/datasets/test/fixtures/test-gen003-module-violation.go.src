package testgen003

import "database/sql"

type Processor struct{ db *sql.DB }

func (p Processor) Process(value string) error {
	_, err := p.db.Exec("insert into events(value) values(?)", value)
	return err
}
