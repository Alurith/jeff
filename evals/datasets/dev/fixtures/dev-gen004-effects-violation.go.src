package gen004

type Store interface {
	Delete(string) error
}

type Reader struct{ store Store }

func (r Reader) Find(id string) (string, error) {
	if err := r.store.Delete(id); err != nil {
		return "", err
	}
	return id, nil
}
