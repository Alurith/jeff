package gen003

import "net/http"

type Service struct{ client *http.Client }

func (s Service) Save(value string) error {
	request, _ := http.NewRequest(http.MethodPost, "https://example.invalid", nil)
	request.Header.Set("X-Value", value)
	_, err := s.client.Do(request)
	return err
}
